const WalletConnect = () => {
  const connectWallet = async () => {
    // connect to Freighter / Stellar wallet
  };

  return (
    <button onClick={connectWallet}>
      Connect Wallet
    </button>
  );
};